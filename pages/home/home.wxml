<!--pages/home/home.wxml-->
<view bindtap="handleViewClick">{{name}}</view>

<view wx:for = "{{students}}">{{item.id}} --{{item.name}}--{{item.age}}</view>

<view>当前计数：{{count}}</view>
<button size="mini" bind:tap="addCount">+</button>

<!-- 2.获取用户信息的方式 -->
<button size="mini"  open-type='getUserInfo' bindgetuserinfo="getUserInfo">获取授权</button>

<!-- 3.获取用户信息的方式 -->
<open-data ></open-data>

<view wx:for="{{list}}">{{item}}</view>